# ⌬ The Ultimate H₂O Calculator 3000

[![License](https://img.shields.io/badge/License-Apache_2.0-blue.svg)](https://opensource.org/licenses/Apache-2.0)
[![Built Without](https://img.shields.io/badge/Built%20Without-eval()-red.svg)](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/eval)
[![Year](https://img.shields.io/badge/Timeline-Year%203000-cyan.svg)](#)
[![Made by](https://img.shields.io/badge/Made%20By-The%20TFSE%20Labs%2E-green.svg)](#)
![Status](https://img.shields.io/badge/STATUS-ACTIVE-yellow.svg)

(*TFS stands for "Tools For Students and Engineers"*)

Welcome to the official repository for **The Ultimate H₂O Calculator 3000** (Version 26.05). This is a neon-drenched, high-performance, responsive scientific calculator engineered for operators demanding absolute mathematical precision wrapped in a cyberpunk aesthetic. 

By ditching legacy execution methods in favor of a robust, tokenized parsing engine, this machine delivers fluid calculation capabilities across desktop and mobile form factors alike.

---

## 🌌 The Philosophy of H₂O Calculation

In a digital landscape cluttered with standard, uninspired UI components, the H₂O Calculator stands as a testament to fluid precision. Just as water adapts perfectly to the geometry of its container, this software architecture seamlessly scales from primary school fraction arithmetic to advanced quantum wavefunction evaluations. 

The glowing neon interface is not mere cosmetic ornament—it reflects the fundamental energy transitions of excited hydrogen atoms. When you engage these controls, you are orchestrating an optimized symphony of binary logic, strict algorithmic safety, and absolute human curiosity.

---

## ⚡ Key Features

* **Zero `eval()` Architecture:** Completely immune to cross-site scripting (XSS) or arbitrary string injection. Tokenization and math logic are driven by a deterministic mathematical core.
* **Shunting-Yard Parser Core:** Implements a custom structural parser to convert traditional infix expressions directly into Reverse Polish Notation (RPN) arrays before execution.
* **Quantum Decimal Safety:** Eradicates standard JavaScript IEEE 754 floating-point rounding anomalies (e.g., $0.1 + 0.2 = 0.30000000000000004$). The calculation pipeline guarantees strict rounding thresholds up to 14 significant digits.
* **Intelligent Scientific Notation:** Massive values or microscopic fractions automatically transition into standardized engineering notations (e.g., $2.5\times10^{15}$) to maintain clarity.
* **Responsive Neon Glassmorphic UI:** Built with dynamic CSS radial gradients, adaptive media queries, hardware-accelerated blur filters, and optimized mobile tap targets.

---

## 🧮 Comprehensive Operator Manual

### 🏫 Basic Arithmetic & Entry (Grades I–III)
For foundational computation, utilize standard numerical inputs along with traditional operators:
* **Operators:** Addition (`+`), Subtraction (`-`), Multiplication (`×`), and Division (`÷`).
* **Grouping:** Explicitly define operator precedence using deep grouping brackets `(` and `)`.
* **Percentage System:** Calculates arbitrary structural ratios ($a\%$ of $b$) safely via optimized processing.

### 📐 Advanced Scientific Functions (Grades IV–XII)
Unlock the secondary analytical tier for higher-level mathematics:
* **Trigonometry:** High-precision evaluation of `sin`, `cos`, and `tan` inputs natively mapped to standard Radian metrics.
* **Inverse Circular Functions:** Calculate arc measurements reliably through built-in domain-guarded `asin`, `acos`, and `atan` functional paths.
* **Logarithmic Suites:** Native operations for both Base-10 common logs (`log`) and Base-$e$ natural log algorithms (`ln`).
* **Exponentials & Radicals:** Execute flexible powers ($x^y$), strict squares ($x^2$), square roots ($\sqrt{x}$), and anti-log foundations ($10^x$, $e^x$).
* **Discrete Operators:** Absolute values ($|x|$), integer-isolated Factorials ($n!$), and structural Modulo remainder calculation (`mod`).

### 🧠 The Volatile Memory Bank
Retain complex cumulative computational fragments during deep execution loops:
* **`MS` (Memory Store):** Captures the current evaluated value and writes it directly to the internal buffer, replacing previous records.
* **`MR` (Memory Recall):** Extracts the preserved memory variable and instantly appends it to the active cursor stream.
* **`M+` / `M-` (Memory Alteration):** Direct arithmetic compilation—adds or subtracts the current active display value to/from the memory block without clearing the queue.
* **`MC` (Memory Clear):** Purges the saved memory ledger back to absolute zero.

---

## 🛠️ Engine Mechanics & Technical Stack

The processing logic relies entirely on semantic web protocols without relying on external dependencies or heavy abstraction layers. 

The engine processes mathematical data sequentially: first running text through the tokenizer unit, reorganizing operators using the Shunting-Yard algorithm, producing a clean Reverse Polish Notation (RPN) stack layout, running those operations through an isolated math execution engine, and piping the validated output to the view container.

### Technical Blueprint
* **Markup Layer:** HTML5 structural nodes leveraging explicit `inputmode="none"` implementations to prevent mobile virtual-keyboard conflicts.
* **Presentational Blueprint:** CSS3 structural grid matrices combined with high-contrast rules to preserve legibility under extreme deep-space or lab environments.
* **Logic Subsystem:** Pure ES6+ JavaScript closure execution models. Standard global environment overrides block malicious text inputs or structural anomalies.

### Standard Keyboard Macros
For desktop-class operators, the environment intercepts physical input arrays automatically:
* **Integers / Points:** `0` through `9` and `.`.
* **Standard Math Matrix:** `+`, `-`, `*`, `/`.
* **Scoping / Powers:** `(`, `)`, `%`, `^`.
* **State Control:** `Enter` (Evaluate Matrix), `Backspace` (Delete Single Token), `Escape` (Global Clear Signal).
* **Inverse Circular Functions:** Calculate arc measurements reliably through built-in domain-guarded `asin`, `acos`, and `atan` functional paths.
* **Logarithmic Suites:** Native operations for both Base-10 common logs (`log`) and Base-$e$ natural log algorithms (`ln`).
* **Exponentials & Radicals:** Execute flexible powers ($x^y$), strict squares ($x^2$), square roots ($\sqrt{x}$), and anti-log foundations ($10^x$, $e^x$).
* **Discrete Operators:** Absolute values ($|x|$), integer-isolated Factorials ($n!$), and structural Modulo remainder calculation (`mod`).

### 🧠 The Volatile Memory Bank
Retain complex cumulative computational fragments during deep execution loops:
* **`MS` (Memory Store):** Captures the current evaluated value and writes it directly to the internal buffer, replacing previous records.
* **`MR` (Memory Recall):** Extracts the preserved memory variable and instantly appends it to the active cursor stream.
* **`M+` / `M-` (Memory Alteration):** Direct arithmetic compilation—adds or subtracts the current active display value to/from the memory block without clearing the queue.
* **`MC` (Memory Clear):** Purges the saved memory ledger back to absolute zero.

---

## 🛠️ Engine Mechanics & Technical Stack

The processing logic relies entirely on semantic web protocols without relying on external dependencies or heavy abstraction layers. 

The engine processes mathematical data sequentially: first running text through the tokenizer unit, reorganizing operators using the Shunting-Yard algorithm, producing a clean Reverse Polish Notation (RPN) stack layout, running those operations through a isolated math execution engine, and piping the validated output to the view container.

### Technical Blueprint
* **Markup Layer:** HTML5 structural nodes leveraging explicit `inputmode="none"` implementations to prevent mobile virtual-keyboard conflicts.
* **Presentational Blueprint:** CSS3 structural grid matrices combined with high-contrast rules to preserve legibility under extreme deep-space or lab environments.
* **Logic Subsystem:** Pure ES6+ JavaScript closure execution models. Standard global environment overrides block malicious text inputs or structural anomalies.

### Standard Keyboard Macros
For desktop-class operators, the environment intercepts physical input arrays automatically:
* **Integers / Points:** `0` through `9` and `.`.
* **Standard Math Matrix:** `+`, `-`, `*`, `/`.
* **Scoping / Powers:** `(`, `)`, `%`, `^`.
* **State Control:** `Enter` (Evaluate Matrix), `Backspace` (Delete Single Token), `Escape` (Global Clear Signal).

---

## 🛡️ License

Distributed under the Apache License, Version 2.0
